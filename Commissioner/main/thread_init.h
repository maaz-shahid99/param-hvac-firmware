#pragma once

/**
 * @brief Initialize OpenThread stack and launch main loop
 * 
 * This function initializes the OpenThread stack and launches
 * the main processing loop. THIS FUNCTION BLOCKS FOREVER.
 */
void thread_init(void);
